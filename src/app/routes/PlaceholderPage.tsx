export function PlaceholderPage({ title }: { title: string }) {
  return (
    <div className="page-card">
      <h1>{title}</h1>
      <p>Bu modül MVP kapsamında temel kabuk olarak eklendi.</p>
    </div>
  );
}
